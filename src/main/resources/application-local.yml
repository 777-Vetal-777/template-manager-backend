security:
  jwt:
    private-key: JPLnpUx4KpRy3yU6

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/dito
    driver-class-name: org.postgresql.Driver
    username: postgres
    password: postgres
  liquibase:
    default-schema: manager
    change-log: classpath:db/changelog/db.changelog-master.xml

logging:
  level:
    liquibase: debug
    org:
      springframework:
        security: debug
    com:
      itextpdf:
        dito:
          manager: debug
