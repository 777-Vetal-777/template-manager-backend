security:
  cors:
    paths: '/**'
    origins: '*'
    methods: '*'

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/dito
    username: postgres
    password: postgres

logging:
  level:
    liquibase: debug
    org:
      springframework:
        security: debug
    com:
      itextpdf:
        dito:
          manager: debug
